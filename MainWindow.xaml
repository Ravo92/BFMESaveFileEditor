<Window x:Class="BFMESaveFileEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BFME2 Savegame Editor"
        Width="1200" Height="800">

    <DockPanel>

        <ToolBar DockPanel.Dock="Top">
            <Button Content="Open" Command="{Binding OpenCommand}"  Margin="8,0,8,0" />
            <Separator/>
            <Button Content="Save As" Command="{Binding SaveAsCommand}" Margin="8,0,8,0" />
            <Separator/>
            <Button Content="Apply Edit" Command="{Binding ApplyEditCommand}"  Margin="8,0,8,0" />
        </ToolBar>

        <Border DockPanel.Dock="Bottom"
        BorderThickness="1,1,1,0"
        BorderBrush="#DDDDDD"
        Padding="8,4"
        Background="#F5F5F5">
            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" />
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="460"/>
            </Grid.ColumnDefinitions>

            <!-- Chunk list -->
            <GroupBox Header="Chunks" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="8">
                <ListBox ItemsSource="{Binding Chunks}"
                         SelectedItem="{Binding SelectedChunk}"
                         DisplayMemberPath="DisplayName"/>
            </GroupBox>

            <!-- Entries table -->
            <GroupBox Header="Entries" Grid.Column="1" Grid.Row="0" Margin="8">
                <DataGrid ItemsSource="{Binding SelectedChunkEntries}"
                      SelectedItem="{Binding SelectedEntry}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding DisplayValue}" Width="*"/>
                        <DataGridTextColumn Header="Offset" Binding="{Binding Offset}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Details / editor -->
            <GroupBox Header="Properties / Upgrades" Grid.Column="2" Grid.Row="0" Margin="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Action row -->
                    <DockPanel Grid.Row="0" Margin="0,0,0,6">
                        <Button Content="+"
                    Command="{Binding AddUpgradeCommand}"
                    Width="28"
                    Height="24"
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right" />
                    </DockPanel>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1"
                  ItemsSource="{Binding SelectedEntryProperties}"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Type" Binding="{Binding Label}" Width="100" IsReadOnly="True"/>

                            <DataGridTemplateColumn Header="Value" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayValue}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ContentControl>
                                            <ContentControl.Style>
                                                <Style TargetType="ContentControl">

                                                    <Setter Property="ContentTemplate">
                                                        <Setter.Value>
                                                            <DataTemplate>
                                                                <TextBox Text="{Binding DisplayValue, UpdateSourceTrigger=PropertyChanged}" />
                                                            </DataTemplate>
                                                        </Setter.Value>
                                                    </Setter>

                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Label}" Value="Upgrade">
                                                            <Setter Property="ContentTemplate">
                                                                <Setter.Value>
                                                                    <DataTemplate>
                                                                        <ComboBox ItemsSource="{Binding DataContext.AvailableUpgrades, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                      SelectedValuePath="."
                                                                      SelectedValue="{Binding DisplayValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                      IsEditable="False" />
                                                                    </DataTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>

                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>